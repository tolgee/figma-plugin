// TODO: Sync visual annotations (badges / overlays) on the canvas with the
// current Tolgee key data. When `options.all` is true, scan the whole document;
// otherwise limit to the current page. Returns the number of updated nodes.
export async function syncAnnotations(
  _options: { all?: boolean },
): Promise<{ updated: number }> {
  // TODO
  return { updated: 0 };
}
