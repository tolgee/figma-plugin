// import createClient from "openapi-fetch";
// import type { paths } from "./schema.generated"; // TODO: nach `pnpm schema` aktivieren

export function createTolgeeClient(_apiUrl: string, _apiKey: string) {
  // TODO: createClient<paths>({
  //   baseUrl: apiUrl + "/v2",
  //   headers: {
  //     "X-API-Key": apiKey,
  //     "X-Tolgee-SDK-Type": "Figma",
  //   },
  // });
  return null;
}
