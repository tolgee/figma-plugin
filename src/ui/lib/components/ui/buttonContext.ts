/**
 * Context key a container can set to pin the default size of any `Button`
 * rendered inside it (unless the button passes an explicit `size`). Lets e.g.
 * `ViewFooter` size a whole row of action buttons from one place.
 */
export const BUTTON_SIZE_CTX = Symbol("button-size");
