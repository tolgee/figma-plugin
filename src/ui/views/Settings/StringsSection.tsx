import { h, FunctionComponent } from "preact";
import { Text } from "@create-figma-plugin/ui";

export const StringsSection: FunctionComponent = () => (
  <Text>To be defined in the future.</Text>
);
